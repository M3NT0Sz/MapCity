# ============================================================================
# EXEMPLO DE CONFIGURAÇÃO DE AMBIENTE - MapCity
# ============================================================================
# Copie este arquivo para .env e configure suas variáveis reais
# ⚠️  NUNCA commite o arquivo .env para o git!

# -----------------------------------------------------------------------------
# SEGURANÇA E AUTENTICAÇÃO
# -----------------------------------------------------------------------------
# JWT Secret Key - Use um valor complexo e único para produção
# Gere uma chave forte: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=sua_chave_jwt_super_secreta_e_complexa_aqui_substitua_por_valor_real

# Salt rounds para hash de senhas (recomendado: 12-15 para produção)
SALT_ROUNDS=12

# -----------------------------------------------------------------------------
# BANCO DE DADOS
# -----------------------------------------------------------------------------
# Configurações do MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=sua_senha_mysql_aqui
DB_NAME=mapcity
DB_CONNECTION_LIMIT=10

# -----------------------------------------------------------------------------
# SERVIDOR
# -----------------------------------------------------------------------------
# Porta do servidor backend
PORT=3001

# URLs permitidas para CORS (separe por vírgula se múltiplas)
ALLOWED_ORIGINS=http://localhost:8081,http://localhost:3000,http://localhost:8083,http://localhost:8082

# -----------------------------------------------------------------------------
# UPLOADS E ARQUIVOS
# -----------------------------------------------------------------------------
# Diretório para uploads de imagens
UPLOAD_DIR=./uploads

# Tamanho máximo de arquivo (em bytes) - 5MB = 5242880
MAX_FILE_SIZE=5242880

# -----------------------------------------------------------------------------
# AMBIENTE
# -----------------------------------------------------------------------------
# Ambiente de execução (development, production, test)
NODE_ENV=development

# Habilitar logs detalhados (true/false)
DEBUG_LOGS=true

# -----------------------------------------------------------------------------
# EXEMPLO DE VALORES PARA DESENVOLVIMENTO
# -----------------------------------------------------------------------------
# Para desenvolvimento local, você pode usar:
# JWT_SECRET=desenvolvimento_nao_usar_em_producao_123456789
# DB_PASSWORD=
# NODE_ENV=development
# DEBUG_LOGS=true

# Para produção, SEMPRE use valores seguros e únicos!
